# Module `pos` — báo cáo lỗi skill (2026-05-06T11:32:39Z)

**Tổng**: 10 | **PASS**: 0 | **Lỗi**: 10

## ❌ LỖI (10)

| Skill | Verdict | Có handler? | Lý do | Snippet |
|---|---|---|---|---|
| `pos_create_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Mình đã thử gọi đúng quy trình để chạy `pos_create_invoice`, nhưng hiện không thể thực thi vì skill này không tồn tại trong index của app hiện tại. - Đã kiểm tra trực tiếp `pos_create_invoice` trên ` |
| `pos_add_payment` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã thử gọi `pos_add_payment`, nhưng hiện không thể thực thi vì skill này không tồn tại trong index khả dụng. - Đã kiểm tra `spaclaw_view_skill` với app `zalo` và nhận `SKILL_NOT_FOUND` - Đã tìm lại t |
| `list_invoices` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì trong app `crm` hiện không có skill `list_invoices`. - Tôi đã kiểm tra trực tiếp skill registry của `crm` với từ khóa `list_invoices` và cả các biến thể `invoice`, `hoa don`,  |
| `view_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được `view_invoice` vì skill này hiện không tồn tại trong index khả dụng. - Đã tra cứu `view_invoice` ở các app có index (`crm`, `page`, `zalo`, `ads`) và đều không có. - Đã thử gọi trực ti |
| `pos_complete_appointment` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill theo yêu cầu vì `pos_complete_appointment` hiện không tồn tại trong app `crm`. - Đã kiểm tra trực tiếp `pos_complete_appointment` trong `crm`: trả về `SKILL_NOT_FOUND` - Đã dò th |
| `apply_voucher_to_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì trong app `crm` hiện không tồn tại skill `apply_voucher_to_invoice`. - Đã tra cứu skill theo đúng tên trong `crm`: không có kết quả - Đã tìm rộng với các từ khóa `voucher`, `i |
| `update_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `update_invoice` vì hệ thống không có skill này. - Đã tra cứu skill trong `crm` với từ khóa `update_invoice` và `invoice`; chỉ thấy `screen_input_invoices`, không có `update_invo |
| `void_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được `void_invoice` vì skill này hiện không tồn tại trong app `crm`. - Tôi đã kiểm tra trực tiếp danh sách skill của `crm` và thử xem chi tiết `void_invoice`. - Kết quả hệ thống trả về `SKI |
| `print_invoice` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Mình đã thử gọi đúng quy trình nhưng không thực thi được vì skill không tồn tại trong các app khả dụng đã kiểm tra. - `spaclaw_list_skills` với `app='zalo'`, query `print_invoice` và `invoice` đều tr |
| `dashboard_overview` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `dashboard_overview` vì hiện tại skill này không tồn tại trong các app đã kiểm tra. - Đã tra cứu `dashboard_overview` ở `crm`, `ads`, `page`, `zalo` → không có skill nào khớp - ` |
