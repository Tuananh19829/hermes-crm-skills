# Module `loyalty` — báo cáo lỗi skill (2026-05-06T11:25:54Z)

**Tổng**: 10 | **PASS**: 0 | **Lỗi**: 10

## ❌ LỖI (10)

| Skill | Verdict | Có handler? | Lý do | Snippet |
|---|---|---|---|---|
| `loyalty_summary` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Mình đã kiểm tra skill `loyalty_summary` nhưng hiện không tồn tại trong các app đã dò (`crm`, `ads`, `page`, `zalo`), nên không thể gọi skill đó ngay. Kết quả tra được về loyalty trong `crm`: - `list |
| `update_loyalty_tier` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã thử gọi theo đúng tên `update_loyalty_tier`, nhưng không thực thi được vì skill này không tồn tại trong app `crm`. - Kết quả tra cứu skill theo `update_loyalty_tier`: không có - Kết quả tra cứu th |
| `delete_loyalty_tier` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì hệ thống không có skill `delete_loyalty_tier`. - Đã tra cứu trong app `crm` và thử tìm rộng theo từ khóa liên quan đến “xóa hạng thẻ / loyalty tier”, kết quả không có skill ph |
| `assign_loyalty_tier` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `assign_loyalty_tier` vì skill này hiện không tồn tại trong app `crm`. - `spaclaw_list_skills` với từ khóa `assign_loyalty_tier` trả về `0` kết quả - `spaclaw_view_skill` cho `as |
| `count_loyalty_customers` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được vì skill `count_loyalty_customers` không tồn tại trong app `crm` của workspace hiện tại. - Tôi đã thử gọi trực tiếp `count_loyalty_customers` trên `crm` và hệ thống trả về `SKILL_NOT_F |
| `list_loyalty_history` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Mình đã thử gọi đúng quy trình nhưng không thể chạy được vì skill này không tồn tại trong app `crm` trên workspace hiện tại. - `spaclaw_list_skills` với từ khóa `list_loyalty_history` trả về `0` kết  |
| `manage_loyalty_label` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã thử gọi đúng quy trình nhưng không thể thực thi vì skill đó không tồn tại trong app `zalo`. - Đã kiểm tra `manage_loyalty_label` trực tiếp: hệ thống trả `SKILL_NOT_FOUND`. - Đã tìm thêm theo các t |
| `manage_loyalty_source` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì trong app `crm` hiện không có skill tên `manage_loyalty_source`. - Đã kiểm tra trực tiếp bằng `spaclaw_list_skills` và `spaclaw_view_skill` cho `manage_loyalty_source` - Kết q |
| `list_loyalty_labels` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `list_loyalty_labels`. - Tôi đã tra cứu skill index ở `zalo`, `crm`, `page`, `ads` với các từ khóa `list_loyalty_labels`, `loyalty`, `label`, `nhãn` nhưng đều không có skill khớp |
| `list_loyalty_sources` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì trong app `crm` hiện không tồn tại `list_loyalty_sources`. - Đã kiểm tra trực tiếp tên skill `list_loyalty_sources` trong `crm`: không tìm thấy - Đã thử tìm skill gần nghĩa ch |
