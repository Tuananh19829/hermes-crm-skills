# Module `funnel` — báo cáo lỗi skill (2026-05-06T11:22:16Z)

**Tổng**: 10 | **PASS**: 1 | **Lỗi**: 9

## ✅ PASS (1)
- `funnel_conversion_report` (NO-HANDLER → đáng nghi PASS)

## ❌ LỖI (9)

| Skill | Verdict | Có handler? | Lý do | Snippet |
|---|---|---|---|---|
| `funnel_summary` | TIMEOUT | ❌ chưa wire | no reply |  |
| `list_funnel_deals` | TIMEOUT | ❌ chưa wire | no reply |  |
| `get_funnel_deal_detail` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill như yêu cầu vì trong app `crm` hiện không tồn tại skill `get_funnel_deal_detail`. - Đã kiểm tra trực tiếp `get_funnel_deal_detail` trong `crm`: hệ thống trả `SKILL_NOT_FOUND`. -  |
| `create_funnel_deal` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì trong app `crm` hiện không tồn tại skill `create_funnel_deal`. - Đã kiểm tra trực tiếp `create_funnel_deal` trong `crm`: không tìm thấy - Đã tìm rộng theo các từ khóa `deal fu |
| `delete_funnel_deal` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `delete_funnel_deal`. - Mình đã tra trong app `crm` với từ khóa `delete_funnel_deal` và cả các biến thể như `xoa deal funnel`, nhưng không có skill nào khớp. - Khi thử xem chi ti |
| `assign_funnel_deal` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `assign_funnel_deal` trong app `crm`. - `spaclaw_list_skills` không tìm thấy skill nào khớp `assign_funnel_deal` - `spaclaw_view_skill` trả về lỗi `SKILL_NOT_FOUND` - Kết quả: ch |
| `bulk_update_funnel_stage` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì hệ thống trả về `SKILL_NOT_FOUND`. - Đã thử tìm trong app `crm` với các truy vấn gồm chính xác `bulk_update_funnel_stage` và các từ khóa liên quan đến bulk/funnel/stage/deal.  |
| `get_funnel_deal_history` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã thử gọi đúng quy trình cho app `crm`, nhưng không thể chạy vì skill không tồn tại trong workspace hiện tại. - Kiểm tra `get_funnel_deal_history`: hệ thống trả `SKILL_NOT_FOUND` - Tìm thêm theo các |
| `manage_funnel_stages` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill này. - Mình đã tra cứu và thử xem chi tiết skill `manage_funnel_stages`, nhưng hệ thống trả `SKILL_NOT_FOUND`. - Đã kiểm tra trong các app `crm`, `ads`, `page`, `zalo` với các từ |
