# Module `services` — báo cáo lỗi skill (2026-05-06T11:37:43Z)

**Tổng**: 11 | **PASS**: 3 | **Lỗi**: 8

## ✅ PASS (3)
- `get_treatment_progress` (NO-HANDLER → đáng nghi PASS)
- `list_bundles` (NO-HANDLER → đáng nghi PASS)
- `create_service`

## ❌ LỖI (8)

| Skill | Verdict | Có handler? | Lý do | Snippet |
|---|---|---|---|---|
| `create_service` | TIMEOUT | ✅ có | no reply |  |
| `search_services` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã gọi thử skill gần tương đương là `list_services` vì `search_services` không tồn tại trong app `crm`. Kết quả: - Tìm thấy 30 dịch vụ active. - Một số dịch vụ nổi bật về da: `Chăm sóc da chuyên sâu` |
| `get_service_detail` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill `get_service_detail` vì skill này hiện không tồn tại trong app `crm` (và cũng không thấy khi dò thêm ở các app khác đã kiểm tra). Kết quả kiểm tra nhanh: - `crm`: không có `get_s |
| `list_service_categories` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được skill vì `list_service_categories` hiện không tồn tại trong app `crm`, và cũng không thấy trong `ads` hoặc `page`. - Đã kiểm tra skill đúng tên `list_service_categories` trong `crm` →  |
| `search_treatments` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không gọi được `search_treatments` vì skill này không tồn tại trong app `crm`. Kết quả tra cứu: - `spaclaw_list_skills` với từ khóa `search_treatments` trả về `0` skill. - `spaclaw_view_skill` cho `s |
| `create_treatment` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Không tìm thấy skill tên `create_treatment`, nên tôi đã gọi skill gần nhất phù hợp là `screen_treatment_form` trong app `crm`. Kết quả: - Skill `screen_treatment_form` là màn hình tạo liệu trình mới  |
| `cancel_service_refund` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã gọi skill `screen_service_cancel` trong app `crm`. - Kết quả: skill là loại `screen`, hệ thống điều hướng tới màn hình hoàn/hủy dịch vụ - URL trả về: `https://crm.spaclaw.pro/#/screen-service-canc |
| `manage_marketing_cost` | NO_SKILL | ❌ chưa wire | LLM nói skill không tồn tại | Đã gọi skill phù hợp gần nhất là `screen_marketing_cost` trong app `crm`. Kết quả: - Skill `manage_marketing_cost` không tồn tại trong danh sách skill hiện có. - Hệ thống tìm thấy skill khớp mục đích |
