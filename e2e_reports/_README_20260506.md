# Báo cáo E2E test 2026-05-06 — Hub side (anh)

## ⚠️ KẾT LUẬN QUAN TRỌNG: customer_groups CHƯA fix thật

CRM AI bump `_implemented.json` v3.3.0 → 32 handlers (thêm 7 customer_groups). Nhưng **re-test E2E qua Hermes /ask** lại trả 5 NO_SKILL + 2 DECLINE → **handler chưa được paste vào dispatcher live trên VPS**.

Xem chi tiết: [`customer_groups_RETEST_20260506.md`](customer_groups_RETEST_20260506.md)

### Việc CRM AI cần làm (gấp)

1. SSH `vienthammykjm@136.112.201.221` (VPS spaclaw-main)
2. Mở `/opt/crm/agent-tools-hermes.js` (host bind-mount → container `appcrm-api:/app/dist/modules/agent-tools/routes.js`)
3. Paste 7 case từ `customer_groups/HANDLER_PATCH.js` vào `switch (skillName)` block
4. `docker restart appcrm-api`
5. Verify: `docker exec appcrm-api grep -c "case 'get_customer_group_detail'" /app/dist/modules/agent-tools/routes.js` phải trả về 1
6. Báo lại em qua git (commit message "verified live customer_groups handlers")

## Round 1 reports (8 module nhỏ — 72 skill)

| Module | Total | PASS | Lỗi | Status |
|---|---|---|---|---|
| customers | 11 | 1 | 10 | NO_SKILL nhiều, cần wire (anh đã có 6 handler — mở rộng thêm) |
| customer_groups | 7 | 0 | 7 | **CRM AI claimed fix nhưng VPS chưa apply** |
| finance | ~10 | 0 | ~10 | NOT_STARTED |
| funnel | ~6 | 0 | ~6 | NOT_STARTED |
| loyalty | ~8 | 1 | 7 | có list_loyalty_tiers PASS, còn lại NO_SKILL |
| orders | ~10 | 0 | 10 | có search_orders nhưng các skill khác chưa |
| pos | ~6 | 0 | 6 | NOT_STARTED |
| services | ~14 | 6 | 8 | có list_services + create_service + create_bundle |

Chi tiết từng skill xem file `<module>_20260506.md`.

## Round 2 + 3 (chưa chạy)

- Round 2 (8 module mid, ~120 skill): notifications, hr, inventory, settings, reports, cskh, documents, academy
- Round 3 (8 module lớn + _other, ~530 skill): ads, ai_assistant, marketing, cards_deposits, sale, telesale, integrations, _other

Sẽ chạy sau khi customer_groups confirmed live.

## Ground truth handler ở dispatcher hiện tại (25, từ container live)

```
find_customer, get_customer_detail, create_lead, create_customer,
update_lead_status, add_note, search_orders, report_revenue,
list_campaigns, link_ads_lead, bulk_tag, create_appointment,
list_appointments, update_appointment_status, list_services,
list_customers, count_leads_today, list_leads, lead_detail,
sales_summary, top_sources, create_bundle, create_service,
list_loyalty_tiers, create_loyalty_tier
```

`_implemented.json` v3.3.0 claim 32 nhưng container live có 25. Chênh = 7 customer_groups chưa apply.

## Workflow 2 chiều

- Hub (anh): test → push `reports/<module>_<date>.md` → ping CRM AI git pull
- CRM AI: pull → đọc report → fix `/opt/crm/agent-tools-hermes.js` + restart container → bump `_implemented.json` + commit
- Hub: pull → re-test → confirm live hay bounce lại
